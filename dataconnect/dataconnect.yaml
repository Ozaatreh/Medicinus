specVersion: "v1beta"
serviceId: "medicinus"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "medicinus-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
